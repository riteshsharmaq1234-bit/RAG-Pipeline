# ===========================================================================
# RAG Document Q&A API - Python dependencies (exact pinned versions)
# ===========================================================================
# Note: bcrypt is pinned to 4.0.1 because passlib 1.7.4 raises a spurious
# "AttributeError: module 'bcrypt' has no attribute '__about__'" warning with
# bcrypt >= 4.1. 4.0.1 avoids the noise while remaining secure.
# ===========================================================================

fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
sentence-transformers==3.3.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pydantic==2.10.4
python-multipart==0.0.20
python-dotenv==1.0.1
openai==1.59.6
